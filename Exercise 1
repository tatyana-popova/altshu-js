ЗАДАЧА
Имеется 3 переменные x = 10, y = 12, и z = 3
Выполните и рассчитайте результат следующих операций для этих переменных:
x += y - x++ * z;
z = --x – y * 5;
y /= x + 5 % z;
z = x++ + y * 5;
x = y - x++ * z;
Сформулируйте вывод о порядке действий.

//x+=y-x++*z
let x = 10;
let y = 12;
let z = 3;

//ДЛИННОЕ РЕШЕНИЕ
//Первым шагом выполняем операцию умонжения x++*z
x++; //x++ постфиксный инкремент, на этом шаге значение x = 10
let multiplication = x++ * z; // на этом шаге x увеличивается на единицу, значение 11
console.log(multiplication); //произведение = 33

//Вторым шагом выполняем операцию сложения x+=y
x = 10 // вовзращаем x исходное значение, иначе из-за постфиксного инкремента он будет равен 12
let addition = x + y;
console.log(addition); //сумма = 22

//Третьим шагом выполняем операцию вычитания (x+=y)-(x++*z)
let subtraction = addition - multiplication;
console.log(subtraction); //разность = -11

//КОРОТКОЕ РЕШЕНИЕ
let iks = x;
x++;
console.log(iks += y - x++ * z); //результат = -11
